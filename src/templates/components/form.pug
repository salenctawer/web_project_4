mixin form()
    .form
        h2.title Edit profile
        form(action="").form-container 
            input(type="text").form__input.form-name
            input(type="text").form__input.form-description
            button(type='submit').form__button Save