mixin element(element)
    .grid-element
        img(src=element.img  alt="").grid-element__img
        .grid-element-container
            h2.grid-element__title=element.title
            img(src=element.like, alt="like")
