include head.pug
include header.pug
include footer.pug
include ../components/profile.pug
block variables
    
doctype html
html(lang='ru')
    +head(page_title || '', scripts || [])

    body.body
        +header()
        main
            +profile()
        +footer()
